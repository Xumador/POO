#include <stdio.h>
#include <math.h>

int main (void) {
  float a, b, c, x1, x2, delta, d;

  scanf("%f %f %f", &a, &b, &c);
  delta = sqrt(d);
  if(delta < 0)
    printf("A equação %.2fx^2",a,"+%.2fx",b,"+%.2f",c);
    return 0;
    if(delta == 0)
      x1 = -b/(2*a);
      if(delta > 0)
        x1 = -b+delta/(2*a);
        x2 = -b-delta/(2*a);
}